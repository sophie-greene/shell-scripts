#!/bin/bash

temp=$(exiftool -genre "$1" | tr -d '[[:space:]]') ;
genre="${temp##*:}";
if [ -z "$genre" ] ; then 
	echo "genre emty deleteing $1"; 
	mv "$1" "/media/sf_ubuntu/genre";
else 
	echo "genre is $genre in $1";
fi

